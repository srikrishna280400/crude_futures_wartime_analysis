•	Tried to analyse crude data for the two month period using basic AI web search feature and formed a very basic bucket classified analysis for day wise repeated patterns across two month period; didn’t work well, trades were still going wrong

•	Asked GPT to generate a comprehensive prompt that imports all kinds of trading data from trading websites through web ways and freely available data online, so the base data for the analysis is very accurate

•	Realized GPT couldn’t itself execute that prompt so used Kimi, Qwen and Claude Sonnet, none of which could execute it perfectly either 

•	Qwen and Kimi hallucinated while Claude hit rate limits

•	Decided on using python code and api keys to directly fetch freely available exposed data from trading websites (this is the start of breaking down the problem into small executable parts)

•	explored upstox api for mcx crude data, but was failing to obtain the api token; explored options of fyers, dhan, icici breeze...fyers asked for reasons and waited for approval, dhan was paid, icici breeze waited approval

•	decided against obtaining mcx sources and moved ahead with international crude wti and brent data 

•	asked gpt to use the template of kimi for py code, which gave a revised version with api keys to plug in 

•	obtained fred, eia, databento api keys with free usage access

•	faced some code issues, fixed

•	databento account got locked on importing huge amounts of trading data through api access

•	chose the cme exchange alternative for data, but again got wait-listed/pay-walled for access

•	chose the alternative of yfinance libraries w/o api keys 

•	implemented some kinda logic that imports data/sends requests in 7 day chunks rather than all at once

•	again fixed some inconsistent logic in the code, to align it accurately with the og-40 prompt 

•	ran the code and it gave all the desired outputs in the desired format in all those files

•	moved onto the next phase of building code that executes logic, back-testing, data analysis of all those files that can be translated into a report and actionable trades with entry-exit strategy 

•	shared all 22 files to gpt to make sure the phase2 code is compatible with the outputs generated by those files 

•	asked gpt for a comprehensive code which encompasses back-testing, pattern-recognition, trading executable strategy, etc etc in one single code 

•	fixed some red lines issues

•	realized the code needed input news files for execution, but there weren’t any, so made it web search and web crawl on its own (self-feeding major relevant news based on logic when drastic price movements have happened)

•	made the code even more stronger and harder so no hallucinations and synthetic news data is filled when failed to find credible sources

•	again fixed some tiny logic drifts in phase2 code

•	explored some options for free data analysis saas that can identify patterns and derive insights on its own w/o on uploading files w/o having to nudge and guide it directionally 

•	fixed tiny red line issues

•	changed phase2 code so it writes back to xlsx files instead of csv ones wherever necessary 

•	confirmed if the present empty fields, columns of xlsx files are okay for phase2 code to run perfectly 

•	realised first mcx relevant data needs to be filled in 22-files output generated by phase 1 code

•	added all upstox api key functionality and logic for fetching mcx data 





**Pt 2 Perp Alt Acc** 

•	asked it to again confirm if phase 1 can be rerun, to fill all mcx, inr relevant data in old output files 

•	implemented all those fixes so mcx, inr empty fields, columns data can be filled by rerunning phase 1 code 

•	realized diff api keys, urls were needed for fetching all diff kinds of mcx data, fixed

•	realised roll over logic for dates within one week is not in the code, fixed

•	fixed red line errors and other recurring snake case identifier misnomer errors

•	realized there was some deviation in spot data fetching logic, since data was being fetching for as far back as 1987 for brent 

•	fixed red line errors and other recurring snake case identifier misnomer errors

•	again realized mistakes in logic since mcx crude prices were being fetched for june contract even for dates in march, fixed

•	realized upstox api key is the sole problem that doesn’t allow to access expired contracts data on the free plan

•	discovered even icici breeze api is useless, provides everything for free except mcx, nse commodities 

•	signed up on angelone for kyc everything, etc for its own truly all free access api

•	changed all the phase1 code according to the angelone api documentation and code logic, installed certain libraries

•	obtained and defined all the env vars of the api keys of my angelone account

•	deployed to vercel, w/o committing to git, to obtain redirect url, done

•	spent a while on how to get a static IP, on digital ocean, oracle cloud, GCP, even though no app required and no order/gtt placement features, etc

•	used the GCP static IP by signing up new on GC, creating VM, promoting its ephemeral IP to static IP

•	got the angelone api, secret, TOTP keys and used them in env file

•	faced some issues with contract logic, rollover logic, unintendedly fetching option chain, etc; fixed

•	realized even angelone doesn’t have any provision for exporting expired contracts data, and all that effort after upstox to angelone migration is all gone to waste

•	silenced all angelone mcx data fetch logic, and only kept the existing partial and full forward back-fill logic as is unchanged 

•	fixed some red line errors after silencing all mcx creation logic 

•	made the code to partial fill only fx rate usd inr relevant blank entries from 1/3/26-8/5/26, and forward fill everything from 9/5/26-27/5/26

•	took a lot of time to make GPT understand this purpose, after many troubleshooting and logic drifts

•	the logic still couldn’t make the fx rate usd inr entries get filled, but entries 8/5/26 onward were filled and fetched data accurately 

•	fixed the session window labeling mismatch with the corresponding fetched data from 12/3/26-29/3/26 to match with the correct and accurate session window labeling of dates from 30/3/26 onward, created a separate makeshift code for this purpose 

•	merged the wti/brent session windows summary data with the og files





**CODEX Session**



•	moved onto CODEX, uploaded all codes, doc files, outputs files, project context and made it to generate a new phase2 code over the existing one to make news fetching more accurate and precise, assign news severity logic, etc etc

•	By combining as much as of the data since mar - now, the price movements with the corresponding news flags....establish some kinda definitive patterns for how many points of swing happen to what extent in what time windows for what kinds of news' according to the scale of news' severity....so the aim is even if price movements dont follow any conventional trading math/logic/patterns....they follow some kinda absurd pattern which is still deducible, that can be capitalized on....alternatively over these absurd patterns are any conventional trading math/logic/patterns superimposed on it as well....another 3rd direction to be explored is, if these two types of patterns not established are there some days where only purely some conventional trading math/logic/patterns are followed.





**Perp Alt Acc**

•	moved back into the GPT 5.4 thinking session, gave the full phase2 code and asked to scrutinize in terms of consistency, alignment and intended purpose w.r.t the phase1 code references and logic

•	fixed all the drifts and red line errors 

•	faced some issues with the output kept troubleshooting and running it over and over 

•	built a certain logic where phase2 code generates its own news input file by scraping the web and pass it downstream on its own, realised such a logic wasn’t already present

•	still faced many issues in connecting to the news sites for date wise news headlines

•	asked GPT to generate a prompt for sonnet 4.6 based on the err faced, diagnose, and then give high level sophisticated-foolproof-logic to get the job done

•	Got the output from Claude and did the surgical changes

•	before running the new phase2 code, re-ran the phase1 code to forward fill only the dates from 27/5/26 onward, worked but spot prices data fetch files failed 

•	so made a new makeshift code to fetch only brent/wti spot prices from mar 1 - may 29, and generate its files, but faced some issues, troubleshooted and fixed

•	built in a logic where spot prices for crude are fetched acc. to the latest, last available dates since code was failing due to hard-coding the end date, worked and files/data were generated exactly as desired

•	ran the phase2 code, after applying the surgical changes from Sonnet, was a partial success, where some failed and some succeeded

•	asked GPT again for surgical changes, by rethinking the approach this time, still didn’t work and faced some new errors

•	prompted claude and applied surgical fixes but that didn’t work too; so ditched the approach of appending news sources to crude data altogether and all of the phase 2 work completely

•	started with the agentic approach since you built the pocket app, described in raw words, then made use of the sophisticated sonnet model to perfect it

•	installed 9 mcp servers so the agentic model easily executes the prompt

•	also made use of faster whispr ai model using terminal command installation, to transcipt-ize the mp4 audio files of full-length market trading related deep dive analysis videos

•	also made use of the gemini prompt, which uses e2b and other sandboxes token efficiently to make the earlier sonnet prompt even better

•	modified the war phases so it is accurate, added custom instructions so that it uses a venv at the specified location, also installs mcp servers if needed beforehand, and proceeded it in a sandbox w/o any permissions at all safely, and also so it never exits the specified folder ever

•	realized you need a provision for a py script, so that dates are auto appended and all the scripts, analysis, dashboards, etc downstream get updated automatically

•	but created a one script runner that updates everything once csv files are updated...which still manually requires updating csv files





